$(document).ready(function(){
  $("tracj-quiz").submit(function(event){
    event.preventDefault();

    // collect the input

    // declare new variables to equal the corresponding modal

    // show match modal

  })
})
